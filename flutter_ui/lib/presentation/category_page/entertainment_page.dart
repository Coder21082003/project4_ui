import 'package:flutter/material.dart';

class EntertainmentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Appliances'),
      ),
      body: Center(
        child: Text('Appliances Category Page'),
      ),
    );
  }
}